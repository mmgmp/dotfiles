#!/bin/bash

# Captura de pantalla
grim /tmp/screen.png

# Editar la foto
convert /tmp/screen.png -paint 1 -swirl 360 /tmp/screen.png

# Bloquear la pantalla con la imagen de fondo
swaylock -i /tmp/screen.png --indicator-idle-visible --indicator-radius 75
