#!/bin/bash

selec=$(find "$PASSWORD_STORE_DIR/otp" -type f -name '*.gpg' -exec basename {} .gpg \; | tofi)
path=$(find "$PASSWORD_STORE_DIR" -type f -name "$selec.gpg" | grep -oP "^$PASSWORD_STORE_DIR/\K.*(?=\.gpg$)")

pass otp -c $path && notify-send "Clave otp copiada"
